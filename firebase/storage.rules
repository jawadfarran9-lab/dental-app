rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TEST PHASE — fully open (matches the open Firestore rules). LOCK DOWN before real launch.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
